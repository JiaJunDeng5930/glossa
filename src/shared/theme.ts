// @constraint glossa.extension_contracts.theme_tokens Extension UI theme tokens keep the shared accent palette consistent across CSS assets and extension-owned page UI.
export { default as GLOSSA_THEME } from "./theme.json";
